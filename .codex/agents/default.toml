name = "default"
description = "General delegated work that does not require a narrower built-in role."
model = "gpt-5.6-terra"
developer_instructions = "Complete the bounded delegated task, preserve project boundaries, verify material claims, and return a concise handoff to the primary agent."
