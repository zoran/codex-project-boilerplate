interface:
  display_name: "Context Retrieval"
  short_description: "Find owners and cross-file repository context"
  default_prompt:
    "Use $context-retrieval to choose exact or semantic discovery, locate repository owners and
    relationships, and read the matched sources directly."

policy:
  allow_implicit_invocation: true
